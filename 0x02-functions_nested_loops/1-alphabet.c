#include "main.h"

/*
 * main prints _putchar
 * return 0 is success
 */

void print_alphabet(void);
{
char alpha = 'a';
while (alpha <= 'z')
	{_putchar(alpha);
	alpha++;
	}
_putchar('\n')
return (0);
}
