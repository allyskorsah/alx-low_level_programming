#include "main.h"

/**
 *_isupper - checks uppercase character
 *@c: the character that is to be checked
 *return: 1 if c is uppercase, 0 if its not
 */
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z'); 
}
